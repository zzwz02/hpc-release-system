"""Auth router — /api/login, /api/logout, /api/whoami, /api/change-password.

# TODO Phase 2 — implement endpoints
"""
from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api", tags=["auth"])


# TODO Phase 2 — POST /api/login
# TODO Phase 2 — POST /api/logout
# TODO Phase 2 — GET  /api/whoami
# TODO Phase 2 — POST /api/change-password
